import 'package:flutter/material.dart';

class UniversityIndonesia extends StatelessWidget {
  const UniversityIndonesia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
